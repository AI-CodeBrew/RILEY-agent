project_id = "oznfpovlmpokkoslrnti"

# Vapi calls these functions directly with the x-vapi-secret header
# (verified in _shared/vapi-auth.ts) — not a Supabase user JWT — so disable
# the platform's JWT check on all three.
[functions.check-agent-availability]
verify_jwt = false

[functions.book-appointment]
verify_jwt = false

[functions.vapi-webhook-handler]
verify_jwt = false

[functions.calendly-webhook-handler]
verify_jwt = false

[functions.vapi-inbound-handler]
verify_jwt = false

# Internal, cron-triggered only (see the migration that schedules it via
# pg_cron/pg_net) — guarded by RECONCILE_CRON_SECRET, not a Vapi/user JWT.
[functions.reconcile-live-calls]
verify_jwt = false
